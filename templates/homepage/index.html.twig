{% extends 'base.html.twig' %}

{% block title %}Hello HomepageController!{% endblock %}

{% block body %}
    {{ dump(session|default('')) }}
    {% if form is defined %}
        {{ form(form) }}
    {% endif %}
{% endblock %}
